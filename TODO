Standard considerations:
- Use cached property
- Optimize code
- Use novalidation manager within methods (such as subnetworks)
- Use jit and numba etc.

First release:
- General:
    - Docs + Tutorials
        - Explain editable install
- Core:
    - Network:
        - update network classifications
        - rename LSAnode to lsa_node

- Vizualize:
    - add sd-plotting

Later releases:
- improved testing
- check raise exceptions in all functions and in docs carefully
- check where notImplemented errors can be added for better tracking of future work
- Migration Guide in docs for other tools.
- Docs:
    - toggle for output.
    - Add folder structurein ascii
- Core:
    - Distances:
        - is_kalmanson should return a circular ordering? or add function that does that
        - permute method for dsitances
    - Networks:
        - Validate MixedPhyNetwork (perhaps remove the second condition from our paper)
        - add undirected network and make mixed a superclass or child?
        - Split binary resolution into out_resolution and in_resolution methods?
        - is_OLP function
        - fix/check eNewick parser and writer
        - phylox conversion
        - Generator:
            - add parallelization of generator construction
            - reuse level (k-1) generators or allow as input?
            - add IOmixin.
            - add network to sdgenerator function.
            - rename construction module and level-k generator function?
    - Quartet:
        - qdistance/nanuq distance, correct weighting and allow other profiles.
    - MSA:
        - laze reading for quicker access
        - logdet distance
    - Split:
        - Use refinement approach/clusters for splitsystem-to-tree function.
        - circular splitsystem classification
    - biopython/phylox conversions
    - add pretty_print function to classes.
    - orient_away_from_vertex better implementation
- Inference:
    - Squirrel:
        - Add a constant 'DEFAULT', with default settings into attr's, then we can also add anotehr settings constant etc.
        - squirrel change order of cycle insertion (not necessarily largest first, but the one with clearest hybrid first)
        - update the qjoining and all other squirrel alg's to take into account nonresolved quartets. (update, omegabar also for non-trivial profiles.)
        - check which squirrel helper methods should use general qpset or sq qpset
        - squirrel could actually take in any set of input trees?
    - Add invariant/algebra support, or add load_from_invariants function
    - CF/gene tree support?
- PANDA: 
    - MAPPD:handle 2-blobs and parallel edges better
    - external scanwidth package & input tree extension is a nxDigraph on the same nodes, or None?
- Visualization:
    - other layouts

Other  ideas:
- random networks using dgenerators and sides, also add randomly iterating R1 and R2 rules

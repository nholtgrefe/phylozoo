Standard things:
- use cached property
- Optimize code
- Use validation manager within methods (such as subnetworks)

Rename phylozoo to phyloweb?
update doc strings to include Raise.
use jit and numba etc.
Validate MixedPhyNetwork (perhaps remove the second condition from our paper), or for now only validate sd network and not m network.
reconsider/standardize imports
Fix eNewick parser and writer / Fix plotting
rename split to base in the file
permute method for dsitances
biopython
Make a network test suite with example networks to be reused etc.
Allow degree-2 nodes?
Is cut-vertex or is articulation?
attribute for splits to store (multiplcitiy, weight etc.)

classificaitons: tree-child (weakly strongly), galled, OLP, is stackfree
use novalidation more


##
- use merged_attr functions for to_sd_network, and move to derivations or not?
- this might mean that we can remove the is_directed parameter from there
- also, we might be able to use a single one for d and sd in a utils module?
